<header id="top-header">
  <span class="siteName">
      <a href="/" title=" ">CSE Motors</a>
  </span>
  <div id="tools">
    <% if (locals.loggedin) { %> <!-- Checking if user is logged in -->
        <div id="logout-links">
          <a href="/account">Welcome <%= locals.accountData.account_firstname %></a>
          <a href="/logout">Logout</a> 
        </div>

        <% } else { %>
        <a href="account/login">My Account</a> 
    <% } %>
  </div>
</header>
